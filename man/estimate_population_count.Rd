% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_population_count.R
\name{estimate_population_count}
\alias{estimate_population_count}
\title{Convert a rate into an absolute patient-count estimate, with uncertainty}
\usage{
estimate_population_count(
  rate,
  rate_se = NULL,
  rate_ci = NULL,
  population,
  distribution = c("beta", "lognormal", "normal"),
  conf_level = 0.95,
  n_sim = 20000
)
}
\arguments{
\item{rate}{Point estimate of the rate (e.g. a prevalence proportion).}

\item{rate_se}{Standard error of \code{rate}. Provide this or \code{rate_ci}.}

\item{rate_ci}{Optional length-2 vector \code{c(low, high)} giving a
\code{conf_level} confidence interval for \code{rate}; used to back out \code{rate_se}
if \code{rate_se} is not supplied directly.}

\item{population}{Population denominator (a single number, or a vector to
get an estimate per stratum/region).}

\item{distribution}{\code{"beta"}, \code{"lognormal"}, or \code{"normal"}; see Description.}

\item{conf_level}{Confidence level for both the input CI (if \code{rate_ci} is
used) and the output CI. Default 0.95.}

\item{n_sim}{Number of Monte Carlo draws.}
}
\value{
A data frame with one row per element of \code{population}: the point
estimate (\code{rate * population}), and the simulated count's mean and
lower/upper confidence bounds.
}
\description{
Multiplies a rate (a prevalence proportion, or an incidence/mortality
rate) by a population denominator to get an absolute patient count, and
propagates the rate's uncertainty through to the count via Monte Carlo
simulation. The sampling distribution for the rate can be chosen to match
what the rate actually is: \code{"beta"} for a proportion bounded on [0, 1]
(recommended for prevalence), \code{"lognormal"} for a strictly positive rate
(recommended for incidence or mortality rates), or \code{"normal"} as a
generic fallback.
}
\examples{
# A prevalence of 8.5\% (95\% CI 7.6-9.4\%) applied to three regions.
estimate_population_count(
  rate = 0.085, rate_ci = c(0.076, 0.094),
  population = c(1.2e6, 3.4e5, 8.9e5),
  distribution = "beta"
)

}
