% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_forward.R
\name{project_forward}
\alias{project_forward}
\title{Project a patient population or burden figure forward in time}
\usage{
project_forward(
  base_count,
  horizon_years,
  pop_growth = 0,
  pop_growth_sd = 0,
  rate_trend = 0,
  rate_trend_sd = 0,
  conf_level = 0.95,
  n_sim = 10000
)
}
\arguments{
\item{base_count}{Current-year patient count (point estimate).}

\item{horizon_years}{Integer, how many years forward to project.}

\item{pop_growth}{Expected annual population growth rate (e.g. \code{0.01} for
1\%/year). Default 0.}

\item{pop_growth_sd}{Standard deviation on \code{pop_growth}, representing
uncertainty in the demographic assumption. Default 0 (treated as fixed).}

\item{rate_trend}{Expected annual trend in the underlying rate (e.g.
\code{0.02} for a rate rising 2\%/year, \code{-0.01} for one falling 1\%/year).
Default 0.}

\item{rate_trend_sd}{Standard deviation on \code{rate_trend}. Default 0.}

\item{conf_level}{Confidence level for the projection interval.}

\item{n_sim}{Number of Monte Carlo trajectories to simulate.}
}
\value{
A data frame with one row per projected year (\code{0} = base year):
the year offset, the point-estimate trajectory, and the simulated
mean and confidence interval.
}
\description{
A lightweight demand-forecasting routine: starting from a current-year
patient count, projects it forward assuming the underlying population
grows (or shrinks) at \code{pop_growth} per year and the underlying rate
drifts at \code{rate_trend} per year, with both assumptions allowed to carry
their own uncertainty, propagated by Monte Carlo simulation. This is a
deliberately simple, transparent projection meant as a starting point for
scenario discussion, not a substitute for a full cohort-component
demographic model or a claims-based uptake curve.
}
\examples{
project_forward(
  base_count = 42000, horizon_years = 5,
  pop_growth = 0.008, pop_growth_sd = 0.002,
  rate_trend = 0.015, rate_trend_sd = 0.01
)

}
