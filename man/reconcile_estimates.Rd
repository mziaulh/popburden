% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_estimates.R
\name{reconcile_estimates}
\alias{reconcile_estimates}
\title{Reconcile independent burden or prevalence estimates into one pooled figure}
\usage{
reconcile_estimates(
  estimate,
  se,
  source = NULL,
  method = c("random", "fixed"),
  conf_level = 0.95
)
}
\arguments{
\item{estimate}{Numeric vector of point estimates, one per source.}

\item{se}{Numeric vector of standard errors, one per source, same length
as \code{estimate}.}

\item{source}{Optional character vector naming each source, used to
label output and plots. Defaults to \verb{Source 1}, \verb{Source 2}, ...}

\item{method}{\code{"fixed"} for inverse-variance-weighted fixed-effect pooling
(assumes all sources estimate the same true quantity and differ only by
sampling error), or \code{"random"} for DerSimonian-Laird random-effects
pooling (allows genuine between-source heterogeneity, e.g. differing
case definitions or catchment periods). Default \code{"random"}, since
independent real-world sources rarely share a single measurement-error
model.}

\item{conf_level}{Confidence level for the pooled interval. Default 0.95.}
}
\value{
An object of class \code{popburden_reconciled}: a list with the pooled
estimate, its standard error and confidence interval, per-source
weights, and (for \code{method = "random"}) the between-source variance
tau-squared, Cochran's Q heterogeneity statistic, and I-squared.
}
\description{
Combines several independent point estimates of the same quantity (a
prevalence, an incidence rate, a patient count) into a single pooled
estimate, using inverse-variance weighting. This is the same machinery
used for fixed- and random-effects meta-analysis, applied here to
reconciling estimates that come from different \emph{sources} (survey,
registry, model-based) rather than different \emph{studies}.
}
\examples{
# Three independent prevalence estimates for the same condition,
# e.g. from a household survey, a claims-based registry, and a
# model-based (GBD-style) estimate.
r <- reconcile_estimates(
  estimate = c(0.081, 0.093, 0.077),
  se       = c(0.006, 0.011, 0.004),
  source   = c("survey", "registry", "model-based")
)
r

}
\references{
DerSimonian R, Laird N (1986). "Meta-analysis in clinical trials."
Controlled Clinical Trials, 7(3), 177-188.

Higgins JPT, Thompson SG (2002). "Quantifying heterogeneity in a
meta-analysis." Statistics in Medicine, 21(11), 1539-1558.
}
