% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_burden_example.R
\name{simulate_burden_example}
\alias{simulate_burden_example}
\title{Simulate an illustrative multi-source, multi-region burden dataset}
\usage{
simulate_burden_example(seed = 20260805)
}
\arguments{
\item{seed}{Random seed, for reproducibility. Default \code{20260805}.}
}
\value{
A list with two data frames:
\describe{
\item{population}{One row per region x age band: \code{region},
\code{age_band}, \code{population}.}
\item{sources}{One row per region x source: \code{region}, \code{source},
\code{estimate} (a prevalence proportion), \code{se}.}
}
}
\description{
Generates a synthetic dataset in the shape this package is designed to
work with: several fictional regions, each with an age-stratified
population, and three independent "sources" of prevalence estimates for
a fictional chronic condition (a household survey, an administrative
registry, and a model-based estimate) with source-appropriate sampling
error. \strong{All figures are simulated for demonstration; they are not
derived from, and are not intended to resemble, any specific real
disease, country, or published estimate.} To use real data, build the
same two data frames (see Value) from WHO, IHME GBD, or national survey
exports.
}
\examples{
d <- simulate_burden_example()
str(d)

}
